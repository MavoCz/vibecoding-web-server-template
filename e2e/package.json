{
  "name": "e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:api": "playwright test --project=api",
    "test:web": "playwright test --project=ui",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "dotenv": "^16.4.0",
    "testcontainers": "^10.16.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "common": "workspace:*"
  }
}
